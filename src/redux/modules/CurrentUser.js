// CurrentUser.js
const LOAD = 'current_user/LOAD'
const UPDATE = 'current_user/UPDATE'
const REMOVE = 'current_user/REMOVE'

const initialState = {
    list: [{
        email: "dev@dev.com",
        password: "devdev123",
        username: "김항해"
    }
    ]
}